version: '3'

services:
  hypno-ai:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./app/static/voices:/app/app/static/voices
      - ./app/static/output:/app/app/static/output
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
